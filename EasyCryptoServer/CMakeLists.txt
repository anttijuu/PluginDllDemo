cmake_minimum_required(VERSION 3.0)
project(EasyCryptoServer)
set(Boost_USE_SHARED_LIBS    ON)
set(Boost_USE_STATIC_LIBX    OFF)
find_package(Boost 1.67.0 REQUIRED COMPONENTS system)
if (Boost_FOUND)
   include_directories(${Boost_INCLUDE_DIRS})
   include_directories(/usr/local/include)
   link_directories(${Boost_LIBRARY_DIR})
   add_executable(EasyCryptoServer main.cpp CryptoServer.cpp jsoncpp.cpp)
   set(CMAKE_CXX_FLAGS "-Wall -std=c++11 -pthread -DEC_DLL ${CMAKE_CXX_FLAGS}")
   find_library(EASYCRYPTO EasyCrypto)
   find_library(BOOST_S Boost::system)
   target_link_libraries(EasyCryptoServer ${EASYCRYPTO})
   target_link_libraries(EasyCryptoServer ${Boost_LIBRARIES})
   # find_library(BOOSTS boost_system)
   # target_link_libraries(EasyCryptoServer Boost::system)
endif()
